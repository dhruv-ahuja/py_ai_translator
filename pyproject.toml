[project]
name = "py-ai-translator"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "crawl4ai>=0.5.0.post4",
    "pydantic-ai-slim[logfire,openai]>=0.0.36",
]
